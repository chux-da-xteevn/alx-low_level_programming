#include <stdio.h>

/**
 * main - in charge of the core functions
 *
 * Return: has 0 for success at all times
 *
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return  (0);
}
